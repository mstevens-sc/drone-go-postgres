pipeline:
  build:
    image: golang:1.8
    commands:
      - sleep 10
      - cd todo
      - go get -v
      - go test -v -cover

services:
  database:
    image: postgres:9
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=todo
